import 'package:flutter/material.dart';

class MyTheme {
  static Color appBar = Colors.teal;
  static Color background = Colors.grey[200];

  static Color error = Colors.pinkAccent[400];
  static Color focus = Colors.teal[200];
  static Color cellDivider = focus;
  static Color controlFocus = Colors.teal[600];
  static Color squareDivider = controlFocus;

  static Color cellBackground = Colors.grey[500];
  static Color control = cellBackground;

  static Color initialValue = Colors.black87;
  static Color normalValue = Colors.white;

  static Color controlText = Colors.white;
  static Color controlTextFocus = Colors.black;
}